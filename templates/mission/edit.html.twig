{% extends 'base.html.twig' %}
{% block title %}
    Modifier la mission {{ mission.noMission }}
{% endblock %}
{% block body %}
    <style>
        .form-container {
            max-width: 900px;
            margin: 0 auto;
        }

        .page-header {
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--border-color);
        }

        .page-header h1 {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .breadcrumb-custom {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1.5rem;
        }

        .breadcrumb-custom a {
            color: var(--gray-color);
            text-decoration: none;
            transition: var(--transition);
        }

        .breadcrumb-custom a:hover {
            color: var(--primary-color);
        }

        .breadcrumb-custom i {
            font-size: 0.8rem;
            color: var(--gray-color);
        }

        .form-card {
            background: white;
            border-radius: 24px;
            box-shadow: var(--shadow-lg);
            overflow: hidden;
            border: 1px solid var(--border-color);
        }

        .form-card-header {
            background: linear-gradient(135deg, rgba(67, 97, 238, 0.05), rgba(58, 12, 163, 0.05));
            padding: 1.5rem 2rem;
            border-bottom: 1px solid var(--border-color);
        }

        .form-card-header h2 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--dark-color);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .form-card-header h2 i {
            color: var(--primary-color);
            font-size: 1.8rem;
        }

        .form-card-body {
            padding: 2rem;
        }

        .form-section {
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px dashed var(--border-color);
        }

        .form-section:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .form-section-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--dark-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .form-section-title i {
            color: var(--primary-color);
            font-size: 1.2rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            font-weight: 500;
            color: var(--dark-color);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.25rem;
        }

        .form-label .required {
            color: var(--danger-color);
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            font-size: 0.95rem;
            transition: all 0.2s ease;
            background-color: white;
        }

        .form-control:hover, .form-select:hover {
            border-color: var(--gray-color);
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 4px rgba(67, 97, 238, 0.1);
            outline: none;
        }

        .input-group {
            display: flex;
            align-items: center;
        }

        .input-group .form-control {
            border-top-right-radius: 0;
            border-bottom-right-radius: 0;
        }

        .input-group-text {
            background: var(--light-gray);
            border: 2px solid var(--border-color);
            border-left: none;
            border-top-right-radius: 12px;
            border-bottom-right-radius: 12px;
            padding: 0.75rem 1rem;
            color: var(--gray-color);
            font-weight: 500;
        }

        .form-actions {
            display: flex;
            justify-content: flex-end;
            gap: 1rem;
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 2px solid var(--border-color);
        }

        .btn {
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.2s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border: none;
            color: white;
            box-shadow: 0 4px 6px rgba(67, 97, 238, 0.2);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 15px rgba(67, 97, 238, 0.3);
        }

        .btn-light {
            background: var(--light-gray);
            border: 2px solid var(--border-color);
            color: var(--gray-color);
        }

        .btn-light:hover {
            background: white;
            border-color: var(--gray-color);
            color: var(--dark-color);
        }

        .btn-danger {
            background: var(--danger-color);
            border: none;
            color: white;
        }

        .btn-danger:hover {
            background: #d32f2f;
            transform: translateY(-2px);
            box-shadow: 0 8px 15px rgba(239, 71, 111, 0.3);
        }

        @media (max-width: 768px) {
            .form-card-body {
                padding: 1.5rem;
            }

            .form-actions {
                flex-direction: column;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
    <div class="container-fluid py-4">
        <div class="form-container">
            <div class="breadcrumb-custom">
                <a href="{{ path('app_mission_index') }}">
                    <i class="fas fa-home me-1"></i>Tableau de Bord
                </a>
                <i class="fas fa-chevron-right"></i>
                <a href="{{ path('app_mission_show', {'id': mission.id}) }}">{{ mission.noMission }}</a>
                <i class="fas fa-chevron-right"></i>
                <span style="color: var(--dark-color); font-weight: 500;">Modifier</span>
            </div>
            <div class="page-header">
                <h1>
                    <i class="fas fa-edit"></i>
                    Modifier la mission
                </h1>
                <p class="text-muted">{{ mission.noMission }} - {{ mission.client.nomClient }}</p>
            </div>
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert" style="border-radius: 12px; margin-bottom: 1.5rem;">
                    <i class="fas fa-check-circle me-2"></i>{{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
            <div class="form-card">
                <div class="form-card-header">
                    <h2>
                        <i class="fas fa-edit"></i>
                        Modification de la mission
                    </h2>
                </div>
                <div class="form-card-body">
                    <form method="post" action="{{ path('app_mission_edit', {'id': mission.id}) }}" id="formEditMission">
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-info-circle"></i>
                                Informations générales
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="client" class="form-label">
                                            Client <span class="required">*</span>
                                        </label>
                                        <select class="form-select" id="client" name="client" required>
                                            <option value="">Sélectionnez un client</option>
                                            {% for client in clients %}
                                                <option value="{{ client.id }}" {% if mission.client and mission.client.id == client.id %}selected{% endif %}>
                                                    {{ client.nomClient }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="type_mission" class="form-label">
                                            Type de mission <span class="required">*</span>
                                        </label>
                                        <select class="form-select" id="type_mission" name="type_mission" required>
                                            <option value="">Sélectionnez un type</option>
                                            {% for type in types_mission %}
                                                <option value="{{ type.id }}" {% if mission.typeMission and mission.typeMission.id == type.id %}selected{% endif %}>
                                                    {{ type.libelleTypeMission }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="date_debut" class="form-label">
                                            Date de début <span class="required">*</span>
                                        </label>
                                        <input type="date" class="form-control" id="date_debut" name="date_debut" 
                                            value="{{ mission.dateDebut|date('Y-m-d') }}" required>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="date_fin" class="form-label">
                                            Date de fin prévue <span class="required">*</span>
                                        </label>
                                        <input type="date" class="form-control" id="date_fin" name="date_fin" 
                                            value="{{ mission.dateFinPrevue|date('Y-m-d') }}" required>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="description" class="form-label">Description détaillée</label>
                                <textarea class="form-control" id="description" name="description" rows="5" 
                                        placeholder="Décrivez la mission, ses objectifs, les livrables attendus...">{{ mission.description }}</textarea>
                            </div>
                        </div>
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-euro-sign"></i>
                                Budget
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="budget_euro" class="form-label">Budget (€)</label>
                                        <div class="input-group">
                                            <input type="number" class="form-control" id="budget_euro" name="budget_euro" 
                                                step="0.01" min="0" placeholder="0.00" value="{{ mission.budgetEuro }}">
                                            <span class="input-group-text">€</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="budget_heures" class="form-label">Budget (heures)</label>
                                        <div class="input-group">
                                            <input type="number" class="form-control" id="budget_heures" name="budget_heures" 
                                                step="0.5" min="0" placeholder="0.00" value="{{ mission.budgetHeures }}">
                                            <span class="input-group-text">h</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-user-tie"></i>
                                Responsable et suivi
                            </div>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="responsable" class="form-label">Responsable de mission</label>
                                        <select class="form-select" id="responsable" name="responsable">
                                            <option value="">Sélectionnez un responsable (optionnel)</option>
                                            {% for responsable in responsables %}
                                                <option value="{{ responsable.id }}" {% if mission.responsable and mission.responsable.id == responsable.id %}selected{% endif %}>
                                                    {{ responsable.prenomEmploye }} {{ responsable.nomEmploye }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="statut" class="form-label">Statut</label>
                                        <select class="form-select" id="statut" name="statut">
                                            <option value="prévue" {% if mission.statut == 'prévue' %}selected{% endif %}>Prévue</option>
                                            <option value="en cours" {% if mission.statut == 'en cours' %}selected{% endif %}>En cours</option>
                                            <option value="en pause" {% if mission.statut == 'en pause' %}selected{% endif %}>En pause</option>
                                            <option value="terminée" {% if mission.statut == 'terminée' %}selected{% endif %}>Terminée</option>
                                            <option value="annulée" {% if mission.statut == 'annulée' %}selected{% endif %}>Annulée</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-tasks"></i>
                                Tâches de la mission
                            </div>
                            <div id="taches-container">
                                {% for tache in mission.taches %}
                                <div class="tache-item card mb-3 p-3">
                                    <div class="d-flex justify-content-between align-items-center mb-2">
                                        <h6 class="mb-0"><i class="fas fa-check-circle me-2 text-primary"></i>Tâche {{ loop.index }}</h6>
                                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="supprimerTache(this)">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                    <div class="row">
                                        <div class="col-md-6 mb-2">
                                            <label class="form-label">Libellé de la tâche *</label>
                                            <input type="text" name="tache_libelle[]" class="form-control" value="{{ tache.libelleTache }}" required>
                                        </div>
                                        <div class="col-md-3 mb-2">
                                            <label class="form-label">Durée estimée (h)</label>
                                            <input type="number" name="tache_duree[]" class="form-control" step="0.5" min="0" value="{{ tache.dureeEstimee }}">
                                        </div>
                                        <div class="col-md-3 mb-2">
                                            <label class="form-label">Priorité</label>
                                            <select name="tache_priorite[]" class="form-select">
                                                {% for priorite in priorites %}
                                                    <option value="{{ priorite }}" {% if tache.priorite == priorite %}selected{% endif %}>{{ priorite|capitalize }}</option>
                                                {% endfor %}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="mb-2">
                                        <label class="form-label">Description</label>
                                        <textarea name="tache_description[]" class="form-control" rows="2">{{ tache.description }}</textarea>
                                    </div>
                                    <div class="competences-container">
                                        <label class="form-label">Compétences requises</label>
                                        <div class="competence-items">
                                            {% for tc in tache.tachesCompetences %}
                                            <div class="row competence-item mb-2">
                                                <div class="col-md-6">
                                                    <select name="tache_competences[{{ loop.parent.loop.index0 }}][]" class="form-select">
                                                        <option value="">Sélectionnez une compétence</option>
                                                        {% for competence in competences %}
                                                            <option value="{{ competence.id }}" {% if tc.competence.id == competence.id %}selected{% endif %}>{{ competence.libelleCompetence }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-4">
                                                    <select name="tache_niveau[{{ loop.parent.loop.index0 }}][]" class="form-select">
                                                        {% for niveau in niveaux %}
                                                            <option value="{{ niveau }}" {% if tc.niveauRequis == niveau %}selected{% endif %}>{{ niveau|capitalize }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-2">
                                                    <button type="button" class="btn btn-sm btn-outline-danger" onclick="supprimerCompetence(this)">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </div>
                                            </div>
                                            {% else %}
                                            <div class="row competence-item mb-2">
                                                <div class="col-md-6">
                                                    <select name="tache_competences[{{ loop.index0 }}][]" class="form-select">
                                                        <option value="">Sélectionnez une compétence</option>
                                                        {% for competence in competences %}
                                                            <option value="{{ competence.id }}">{{ competence.libelleCompetence }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-4">
                                                    <select name="tache_niveau[{{ loop.index0 }}][]" class="form-select">
                                                        {% for niveau in niveaux %}
                                                            <option value="{{ niveau }}">{{ niveau|capitalize }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-2">
                                                    <button type="button" class="btn btn-sm btn-outline-success" onclick="ajouterCompetence(this, {{ loop.index0 }})">
                                                        <i class="fas fa-plus"></i>
                                                    </button>
                                                </div>
                                            </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                                {% else %}
                                <div class="tache-item card mb-3 p-3">
                                    <div class="d-flex justify-content-between align-items-center mb-2">
                                        <h6 class="mb-0"><i class="fas fa-check-circle me-2 text-primary"></i>Tâche 1</h6>
                                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="supprimerTache(this)">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                    <div class="row">
                                        <div class="col-md-6 mb-2">
                                            <label class="form-label">Libellé de la tâche *</label>
                                            <input type="text" name="tache_libelle[]" class="form-control" required>
                                        </div>
                                        <div class="col-md-3 mb-2">
                                            <label class="form-label">Durée estimée (h)</label>
                                            <input type="number" name="tache_duree[]" class="form-control" step="0.5" min="0">
                                        </div>
                                        <div class="col-md-3 mb-2">
                                            <label class="form-label">Priorité</label>
                                            <select name="tache_priorite[]" class="form-select">
                                                {% for priorite in priorites %}
                                                    <option value="{{ priorite }}" {% if priorite == 'moyenne' %}selected{% endif %}>{{ priorite|capitalize }}</option>
                                                {% endfor %}
                                            </select>
                                        </div>
                                    </div>
                                    <div class="mb-2">
                                        <label class="form-label">Description</label>
                                        <textarea name="tache_description[]" class="form-control" rows="2"></textarea>
                                    </div>
                                    <div class="competences-container">
                                        <label class="form-label">Compétences requises</label>
                                        <div class="competence-items">
                                            <div class="row competence-item mb-2">
                                                <div class="col-md-6">
                                                    <select name="tache_competences[0][]" class="form-select">
                                                        <option value="">Sélectionnez une compétence</option>
                                                        {% for competence in competences %}
                                                            <option value="{{ competence.id }}">{{ competence.libelleCompetence }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-4">
                                                    <select name="tache_niveau[0][]" class="form-select">
                                                        {% for niveau in niveaux %}
                                                            <option value="{{ niveau }}">{{ niveau|capitalize }}</option>
                                                        {% endfor %}
                                                    </select>
                                                </div>
                                                <div class="col-md-2">
                                                    <button type="button" class="btn btn-sm btn-outline-success" onclick="ajouterCompetence(this, 0)">
                                                        <i class="fas fa-plus"></i>
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                {% endfor %}
                            </div>
                            <div class="mt-3">
                                <button type="button" class="btn btn-outline-primary" onclick="ajouterTache()">
                                    <i class="fas fa-plus-circle me-2"></i>Ajouter une tâche
                                </button>
                            </div>
                        </div>
                        <div class="form-actions">
                            <a href="{{ path('app_mission_show', {'id': mission.id}) }}" class="btn btn-light">
                                <i class="fas fa-times"></i>
                                Annuler
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i>
                                Enregistrer les modifications
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    {% block javascripts %}
        {{ parent() }}
        <script>
            let tacheCount = {{ mission.taches|length }};
            let competenceCounters = [];
            {% for tache in mission.taches %}
                competenceCounters[{{ loop.index0 }}] = {{ tache.tachesCompetences|length }};
            {% endfor %}
            const priorites = {{ priorites|json_encode|raw }};
            const competences = [{% for competence in competences %}{ id: {{ competence.id }}, libelleCompetence: "{{ competence.libelleCompetence|e('js') }}" },{% endfor %}];
            const niveaux = {{ niveaux|json_encode|raw }};
            function ajouterTache()
            {
                tacheCount++;
                competenceCounters[tacheCount - 1] = 0;
                const container = document.getElementById('taches-container');
                const newTache = document.createElement('div');
                newTache.className = 'tache-item card mb-3 p-3';
                let prioriteOptions = '';
                priorites.forEach(priorite =>
                {
                    const selected = priorite === 'moyenne' ? 'selected' : '';
                    prioriteOptions += `<option value="${priorite}" ${selected}>${priorite.charAt(0).toUpperCase() + priorite.slice(1)}</option>`;
                });
                let competenceOptions = '<option value="">Sélectionnez une compétence</option>';
                competences.forEach(competence =>
                {
                    competenceOptions += `<option value="${competence.id}">${competence.libelleCompetence}</option>`;
                });
                let niveauOptions = '';
                niveaux.forEach(niveau =>
                {
                    niveauOptions += `<option value="${niveau}">${niveau.charAt(0).toUpperCase() + niveau.slice(1)}</option>`;
                });
                newTache.innerHTML = `
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="mb-0"><i class="fas fa-check-circle me-2 text-primary"></i>Tâche ${tacheCount}</h6>
                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="supprimerTache(this)">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-2">
                            <label class="form-label">Libellé de la tâche *</label>
                            <input type="text" name="tache_libelle[]" class="form-control" required>
                        </div>
                        <div class="col-md-3 mb-2">
                            <label class="form-label">Durée estimée (h)</label>
                            <input type="number" name="tache_duree[]" class="form-control" step="0.5" min="0">
                        </div>
                        <div class="col-md-3 mb-2">
                            <label class="form-label">Priorité</label>
                            <select name="tache_priorite[]" class="form-select">
                                ${prioriteOptions}
                            </select>
                        </div>
                    </div>
                    
                    <div class="mb-2">
                        <label class="form-label">Description</label>
                        <textarea name="tache_description[]" class="form-control" rows="2"></textarea>
                    </div>
                    
                    <div class="competences-container">
                        <label class="form-label">Compétences requises</label>
                        <div class="competence-items">
                            <div class="row competence-item mb-2">
                                <div class="col-md-6">
                                    <select name="tache_competences[${tacheCount-1}][]" class="form-select">
                                        ${competenceOptions}
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <select name="tache_niveau[${tacheCount-1}][]" class="form-select">
                                        ${niveauOptions}
                                    </select>
                                </div>
                                <div class="col-md-2">
                                    <button type="button" class="btn btn-sm btn-outline-success" onclick="ajouterCompetence(this, ${tacheCount-1})">
                                        <i class="fas fa-plus"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
                container.appendChild(newTache);
            }

            function supprimerTache(button)
            {
                const tacheItem = button.closest('.tache-item');
                if(document.querySelectorAll('.tache-item').length > 1)
                {
                    tacheItem.remove();
                }
                else
                {
                    alert('Vous devez garder au moins une tâche.');
                }
            }

            function ajouterCompetence(button, tacheIndex)
            {
                const competenceItems = button.closest('.competence-items');
                const newCompetence = document.createElement('div');
                newCompetence.className = 'row competence-item mb-2';
                const counter = competenceCounters[tacheIndex] || 0;
                competenceCounters[tacheIndex] = counter + 1;
                let competenceOptions = '<option value="">Sélectionnez une compétence</option>';
                competences.forEach(competence =>
                {
                    competenceOptions += `<option value="${competence.id}">${competence.libelleCompetence}</option>`;
                });
                let niveauOptions = '';
                niveaux.forEach(niveau =>
                {
                    niveauOptions += `<option value="${niveau}">${niveau.charAt(0).toUpperCase() + niveau.slice(1)}</option>`;
                });
                newCompetence.innerHTML = `
                    <div class="col-md-6">
                        <select name="tache_competences[${tacheIndex}][]" class="form-select">
                            ${competenceOptions}
                        </select>
                    </div>
                    <div class="col-md-4">
                        <select name="tache_niveau[${tacheIndex}][]" class="form-select">
                            ${niveauOptions}
                        </select>
                    </div>
                    <div class="col-md-2">
                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="supprimerCompetence(this)">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                `;
                competenceItems.appendChild(newCompetence);
            }

            function supprimerCompetence(button)
            {
                const competenceItem = button.closest('.competence-item');
                if(competenceItem.parentElement.children.length > 1)
                {
                    competenceItem.remove();
                }
            }

            document.addEventListener('DOMContentLoaded', function()
            {
                const form = document.getElementById('formEditMission');
                const dateDebut = document.getElementById('date_debut');
                const dateFin = document.getElementById('date_fin');
                form.addEventListener('submit', function(e)
                {
                    if(new Date(dateDebut.value) > new Date(dateFin.value))
                    {
                        e.preventDefault();
                        alert('La date de fin doit être postérieure à la date de début.');
                        return;
                    }
                    const taches = document.querySelectorAll('.tache-item');
                    let hasError = false;
                    taches.forEach((tache, index) =>
                    {
                        const libelle = tache.querySelector('input[name="tache_libelle[]"]');
                        if(!libelle.value.trim())
                        {
                            e.preventDefault();
                            alert(`La tâche ${index + 1} doit avoir un libellé.`);
                            hasError = true;
                            return;
                        }
                    });
                    if (hasError) return;
                });
                dateDebut.addEventListener('change', function() {
                    dateFin.min = this.value;
                    if(dateFin.value && new Date(dateFin.value) < new Date(this.value))
                    {
                        dateFin.value = this.value;
                    }
                });
            });
        </script>
    {% endblock %}
{% endblock %}